import React, {FC} from 'react';
import {SearchInput} from 'ut-component';

const SearchInputScreen: FC = props => {
  return <SearchInput title="输入框" placeholderText="请输入" />;
};

export default SearchInputScreen;
